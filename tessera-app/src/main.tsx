import React from "react";
import ReactDOM from "react-dom/client";
import Tessera from "./Tessera.tsx";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <Tessera />
  </React.StrictMode>,
);
